FROM node

WORKDIR /app

COPY package.json .

RUN npm install

COPY . .

EXPOSE 80

ENV MAX_POOL=5
ENV MIN_POOL=0
ENV ACQUIRE_POOL=30000
ENV IDLE_POOL=10000
ENV HOST=172.81.3.95
ENV USER=root
ENV PASSWORD=root
ENV DB=db

CMD ["npm", "start"]